std := import('std')
cst := import('cst')
req := import('req')
gen := import('gen')
mygen := import('mygen')

{ lru: lru } := import('lru')

// ---

r := gen.RandomGenerator(0)
r |> std.println()

gen.createRandomSequence(?, 100, r) |> std.println()
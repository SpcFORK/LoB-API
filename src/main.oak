std := import('std')
cst := import('cst')
req := import('req')
gen := import('gen')

{ lru: lru } := import('lru')

// ---

r := req.Anglishize(0, 1, 1, 1, 1)
r |> std.println()

req.reqAnglishize(r) |> std.println()